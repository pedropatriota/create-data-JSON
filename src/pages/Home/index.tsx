import * as Styled from "./styles";

const Home = () => {
  return (
    <Styled.Container>
      <Styled.FavoriteContainer>home</Styled.FavoriteContainer>
    </Styled.Container>
  );
};

export default Home;
